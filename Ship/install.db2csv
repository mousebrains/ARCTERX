#! /bin/bash
#
# Install ship DB to CSV service and timer
#
# June-2023, Pat Welch, pat@mousebrains.com

myPath=`dirname $0`

$myPath/TPWUtils/install.py \
	--verbose \
	--service=shipDB2CSV.service \
	$*
